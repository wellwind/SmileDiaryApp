<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SmileDiaryApp.Views.TakePicturePage"
             Title="取得微笑">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0, 20, 0, 0" />
    </ContentPage.Padding>

    <StackLayout VerticalOptions="Start">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Text="拍張新照片" Command="{Binding TakePictureCommand}" />
            <Button Grid.Column="1" Text="從相簿選擇" Command="{Binding SelectFromAlbumCommand}" />
        </Grid>
        <Button Text="就決定是你了!" Command="{Binding SelectFromAlbumCommand}" />
        <Image Source="{Binding Photo}" />
    </StackLayout>
</ContentPage>